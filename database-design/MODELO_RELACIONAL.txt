

MODELO RELACIONAL


Reglas de normalización aplicadas, cumple con 3FN.

Books (bookID(PK), title NOT NULL, description, published, categories, cover, pages, isbn(Unique))

Writers (writerID(PK), name)

Books_Writers ((bookID(FK) NOT NULL, writerID(FK) NOT NULL)(PK))

Shelves (shelvesID(PK), name)

Books_Shelves ((bookID(FK) NOT NULL, shelvesID(FK) NOT NULL)(PK))

BookStatus (bookID(PK)(FK), progress, status, rating, startReading, endReading)

Notes (notesID(PK), text, created, LibroID(FK) NOT NULL)

